{
  "name": "cursor(1).js",
  "version": "1.0.0",
  "scripts": {
    "minify": "terser cursor(1).js -o dist/cursor(1).min.js --compress --mangle",
    "gzip": "gzip -k cursor(1).min.js"
  },
  "devDependencies": {
    "terser": "^5.0.0"
  }
}
